const array = [15, 16, 17, 18, 19];

function reducer(accmulator, currentValue, index) {
    const return = accmulator + currentValue;
    console.log(
        'accmulator: ${accmulator}, currentValue; ${currentValue}, index: ${index}, returns; ${return}',);
        return returns;
    }

array.reduce(reduce);